# ⚡ Ускорение с моделью Medium

## 🎯 Ваша ситуация

- **Модель:** medium (1.42 GB) - нужна для качества
- **Проблема:** Медленно (6.6 минут)
- **Вопрос:** Будет ли быстрее, если перенести модель на сервер?

## 💡 Ответ: Да, но важно КАК перенести

### Вариант 1: Модель уже на сервере (текущая ситуация)

**Модель уже локально:** `E:\whisper-models\medium.pt` (1.42 GB)

**Проблема:** Используется стандартный Whisper (медленный на CPU)

**Что можно сделать:**

#### A. Использовать Faster-Whisper (4-5x быстрее!) ⭐ РЕКОМЕНДУЕТСЯ

Faster-Whisper - это оптимизированная версия Whisper, которая использует CTranslate2.

**Установка:**
```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1
pip install faster-whisper
```

**Что произойдет:**
- Faster-Whisper скачает свою версию модели medium (CTranslate2 формат)
- Эта версия оптимизирована и работает в 4-5x быстрее
- Размер модели примерно тот же (~1.4 GB)

**Ожидаемое ускорение:**
- Было: 6.6 минут
- Станет: ~1.5-2 минуты ⚡

**Важно:** XET уже отключен в коде, поэтому скачивание должно работать.

---

#### B. Оптимизировать параметры (2-3x быстрее)

В интерфейсе или через API можно использовать:
- `beam_size=1` вместо 5 (быстрее, но немного менее точно)
- `best_of=1` вместо 5 (быстрее)

**Ожидаемое ускорение:**
- Было: 6.6 минут
- Станет: ~2-3 минуты

**Комбинация:** Faster-Whisper + оптимизированные параметры = еще быстрее!

---

### Вариант 2: Перенести на другой сервер (GPU/мощный CPU)

Если у вас есть доступ к серверу с:
- **NVIDIA GPU с CUDA** - ускорение в 10-30x!
- **Мощным CPU** (много ядер) - ускорение в 2-3x

**Что нужно:**
1. Скопировать модель `medium.pt` на сервер
2. Установить зависимости (whisper, faster-whisper)
3. Если есть GPU - установить CUDA версию PyTorch

---

## 📊 Сравнение вариантов для Medium модели

| Вариант | Скорость | Сложность | Качество |
|---------|----------|-----------|----------|
| **Текущее (стандартный Whisper)** | 1x (6.6 мин) | - | ⭐⭐⭐⭐⭐ |
| **Faster-Whisper** | 4-5x (1.5-2 мин) | ⭐ Легко | ⭐⭐⭐⭐⭐ |
| **Faster-Whisper + параметры** | 6-8x (~1 мин) | ⭐ Легко | ⭐⭐⭐⭐ |
| **GPU сервер** | 10-30x (20-40 сек) | ⭐⭐⭐ Сложно | ⭐⭐⭐⭐⭐ |

---

## 🚀 Рекомендация

### Шаг 1: Установите Faster-Whisper (СЕЙЧАС)

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1
pip install faster-whisper
```

### Шаг 2: Перезапустите сервер

```powershell
.\run_server.ps1
```

### Шаг 3: Попробуйте конвертировать

Faster-Whisper:
1. Скачает оптимизированную версию medium модели
2. Будет работать в 4-5x быстрее
3. Сохранит качество распознавания

---

## ⚙️ Что происходит технически

### Стандартный Whisper (текущее):
- Использует PyTorch
- Обрабатывает на CPU
- Не оптимизирован для скорости
- **Скорость:** ~1x реального времени

### Faster-Whisper:
- Использует CTranslate2 (оптимизированный движок)
- Квантование (int8 вместо float32)
- Оптимизированные операции
- **Скорость:** ~4-5x реального времени (CPU)

### Faster-Whisper на GPU:
- Те же оптимизации + CUDA
- Параллельная обработка на GPU
- **Скорость:** ~10-30x реального времени

---

## 🔍 Проверка после установки Faster-Whisper

После установки и перезапуска сервера в логах должно быть:
```
Используется: Faster-Whisper
```

А не:
```
Используется: Standard Whisper
```

---

## ✨ Итог

**Модель medium уже "на сервере"** (локально в `E:\whisper-models\medium.pt`).

**Для ускорения:**
1. ✅ **Установите Faster-Whisper** - даст 4-5x ускорение
2. ✅ **Оптимизируйте параметры** - еще 2x ускорение
3. ⚠️ **Используйте GPU сервер** - 10-30x ускорение (если есть доступ)

**Попробуйте Faster-Whisper - это самое простое решение!**



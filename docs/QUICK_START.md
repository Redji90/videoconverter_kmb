# Быстрый старт

## 🚀 Запуск приложения

### Быстрый запуск (если уже установлено)

Если зависимости уже установлены, просто запустите оба сервера:

**Бэкенд (порт 8000):**
```powershell
# Windows PowerShell
cd backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# или используйте скрипт:
.\run_server.ps1
```

```bash
# macOS/Linux
cd backend
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Фронтенд (порт 5173):**
```bash
cd frontend
npm run dev
```

**После запуска:**
- Откройте браузер: `http://localhost:5173`
- API документация: `http://localhost:8000/docs`

---

## Минимальная установка (5 минут)

### Шаг 1: Установите зависимости системы
```bash
# FFmpeg (обязательно!)
# Windows: choco install ffmpeg
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg
```

### Шаг 2: Backend
```powershell
# PowerShell (Windows)
cd backend
python -m venv venv

# Активация виртуального окружения в PowerShell:
.\venv\Scripts\Activate.ps1

# Если получаете ошибку о политике выполнения, выполните:
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

pip install -r requirements.txt
```

```bash
# macOS/Linux (Bash)
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 3: Frontend
```bash
cd frontend
npm install
```

### Шаг 4: Запуск

#### Вариант 1: Ручной запуск (Windows PowerShell)

**Терминал 1 - Запуск бэкенда:**
```powershell
cd backend

# Активация виртуального окружения
.\venv\Scripts\Activate.ps1

# Запуск сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Или используйте готовый скрипт:
.\run_server.ps1
```

**Терминал 2 - Запуск фронтенда:**
```powershell
cd frontend
npm run dev
```

#### Вариант 2: Ручной запуск (macOS/Linux)

**Терминал 1 - Запуск бэкенда:**
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Терминал 2 - Запуск фронтенда:**
```bash
cd frontend
npm run dev
```

#### Вариант 3: Использование скриптов (Windows PowerShell)

**Для бэкенда:**
```powershell
cd backend
.\run_server.ps1
```

**Для фронтенда:**
```powershell
cd frontend
npm run dev
```

### Шаг 5: Проверка запуска

После запуска обоих серверов:

1. **Бэкенд** будет доступен по адресу: `http://localhost:8000`
   - Проверка: откройте `http://localhost:8000/health` в браузере
   - Документация API: `http://localhost:8000/docs` (Swagger UI)

2. **Фронтенд** будет доступен по адресу: `http://localhost:5173`
   - Откройте этот адрес в браузере для использования приложения

---

## 🔍 Проверка работы

### Проверка бэкенда

1. Откройте в браузере: `http://localhost:8000/health`
   - Должен вернуться: `{"status": "healthy"}`

2. Проверка API: `http://localhost:8000/docs`
   - Должна открыться интерактивная документация Swagger

3. В консоли бэкенда должно быть:
   ```
   INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
   INFO:     Started reloader process
   INFO:     Started server process
   INFO:     Waiting for application startup.
   ✓ Используется оптимизированный сервис распознавания
   ✓ Путь к моделям HuggingFace: E:\huggingface-cache
   ```

### Что ожидать при первом запуске

**Нормальные сообщения при запуске:**

1. **Сообщения о путях моделей:**
   ```
   ✓ Модели HuggingFace (diarization) будут сохраняться в: E:\huggingface-cache
   Используется HF_HOME: E:\huggingface-cache
   ```
   Это означает, что модели будут сохраняться на диск E (если настроено).

2. **Предупреждения (Warnings) - можно игнорировать:**
   ```
   UserWarning: torchaudio._backend.list_audio_backends has been deprecated...
   ```
   Эти предупреждения не критичны - библиотеки используют устаревший API, но всё работает корректно.

3. **Загрузка моделей при первом использовании:**
   При первом конвертировании видео с опцией "Разделение по ролям" начнется загрузка модели:
   ```
   model.bin:   0%|          | 0.00/145M [00:00<?, ?B/s]
   ```
   Это нормально - просто подождите завершения загрузки (может занять несколько минут в зависимости от скорости интернета).

### Проверка фронтенда

1. Откройте в браузере: `http://localhost:5173`
   - Должен загрузиться интерфейс приложения

2. В консоли фронтенда должно быть:
   ```
   VITE v5.x.x  ready in xxx ms
   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

### Частые проблемы

**Проблема: "Port 8000 is already in use"**
- Решение: Закройте другой процесс на порту 8000 или измените порт:
  ```bash
  uvicorn app.main:app --reload --port 8001
  ```

**Проблема: "Port 5173 is already in use"**
- Решение: Vite автоматически предложит использовать другой порт, или измените вручную:
  ```bash
  npm run dev -- --port 5174
  ```

**Проблема: Бэкенд не запускается**
- Проверьте, активировано ли виртуальное окружение
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

**Проблема: Фронтенд не подключается к бэкенду**
- Убедитесь, что бэкенд запущен на `http://localhost:8000`
- Проверьте настройки прокси в `frontend/vite.config.ts`

**Проблема: Вижу предупреждения (Warnings) в консоли**
- Предупреждения о `torchaudio._backend.list_audio_backends` можно игнорировать
- Это не ошибки, библиотеки используют устаревший API, но всё работает корректно

**Проблема: Идет загрузка модели, что делать?**
- Это нормально при первом использовании функции "Разделение по ролям"
- Просто подождите - модель загружается автоматически
- После первой загрузки она сохранится в кэше и больше не будет скачиваться
- Размер модели diarization: ~1.5 GB, время загрузки зависит от скорости интернета

**✅ НОВОЕ: Простая diarization (работает сразу, без моделей!)**
- Просто включите "Разделение по ролям" в интерфейсе - работает сразу!
- Не требует скачивания моделей, использует анализ пауз
- Подходит для диалогов и интервью
- **Подробнее:** `backend/DIARIZATION_WORKING_SOLUTION.md`

**Хотите загрузить модель diarization заранее (высокая точность)?**
- См. инструкцию: `backend/DOWNLOAD_DIARIZATION_MODEL.md`
- **Важно:** Требуется токен HuggingFace (см. инструкцию)
- Быстрый способ: `cd backend` → `.\download_diarization.ps1`
- **Альтернатива:** Ручная загрузка через браузер: `backend/MANUAL_MODEL_DOWNLOAD.md`
- **Другие модели:** См. альтернативы: `backend/DIARIZATION_ALTERNATIVES_SIMPLE.md`

**Если модели не скачиваются - используйте стандартный Whisper:**
- См. рабочее решение: `backend/FAST_WORKING_SETUP.md`
- Удалите faster-whisper: `pip uninstall faster-whisper -y`
- Будет использоваться ваш `medium.pt` - работает сразу!

**Проблема: Модель уже скачана (например, medium.pt), но загружается снова**
- Это может быть связано с разными форматами моделей:
  - Стандартный Whisper использует `.pt` файлы
  - Faster-Whisper использует другой формат (CTranslate2)
  - Если используется Faster-Whisper, он не может использовать `.pt` файлы напрямую
- **Подробное объяснение:** см. `backend/MODEL_FORMATS_EXPLAINED.md`
- **Быстрое решение:** Если видите `model.bin` - это модель для diarization (из HuggingFace), это нормально и не связано с Whisper моделями

---

## 📥 Предварительная загрузка модели diarization

Если хотите загрузить модель для разделения по ролям заранее (чтобы не ждать при использовании):

**Способ 1: PowerShell скрипт (самый простой)**
```powershell
cd backend
.\download_diarization.ps1
```

**Способ 2: Python скрипт**
```powershell
cd backend
.\venv\Scripts\Activate.ps1
python download_diarization_model.py
```

**Подробная инструкция:** `backend/DOWNLOAD_DIARIZATION_MODEL.md`

---

## Тестирование

1. Загрузите любое видео (MP4, AVI, MOV)
2. Выберите язык (или авто)
3. Выберите модель (начните с "base")
4. Нажмите "Конвертировать"
5. Дождитесь результата (может занять время при первом запуске - скачивается модель)

---

## Альтернативные варианты реализации

См. `VARIANTS_COMPARISON.md` для других вариантов:
- Flask + Vue.js + SpeechRecognition (проще)
- Node.js + AssemblyAI (быстрее, но платно)
- Django + Vosk (офлайн)


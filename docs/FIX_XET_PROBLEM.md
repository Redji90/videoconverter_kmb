# 🔧 Решение проблемы XET (зависание загрузки моделей)

## 🎯 Проблема

При попытке скачать модель Faster-Whisper возникает ошибка:
```
Error while downloading from ... cas-bridge.xethub.hf.co: Read timed out.
Xet Storage is enabled for this repo, but the 'hf_xet' package is not installed.
```

XET на Windows часто вызывает зависание загрузок больших файлов.

## ✅ Решение 1: XET отключен автоматически (УЖЕ ИСПРАВЛЕНО)

Код автоматически отключает XET при запуске. Просто перезапустите сервер:

```powershell
cd C:\prj\converter\backend
.\run_server.ps1
```

---

## ✅ Решение 2: Использовать стандартный Whisper (БЫСТРОЕ РЕШЕНИЕ)

Если проблема повторяется, используйте стандартный Whisper (у вас уже есть `medium.pt`):

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1

# Удалить faster-whisper
pip uninstall faster-whisper -y

# Перезапустить сервер
.\run_server.ps1
```

**Преимущества:**
- ✅ Работает сразу с вашей моделью `medium.pt`
- ✅ Не нужно ничего скачивать
- ✅ Нет проблем с XET

---

## ✅ Решение 3: Отключить XET вручную

Если хотите оставить faster-whisper, но отключить XET:

```powershell
# В PowerShell перед запуском сервера
$env:HF_HUB_ENABLE_HF_TRANSFER="0"
cd C:\prj\converter\backend
.\run_server.ps1
```

Или добавьте в `backend/run_server.ps1`:

```powershell
# В начало файла run_server.ps1
$env:HF_HUB_ENABLE_HF_TRANSFER="0"
```

---

## 📝 Что было исправлено

В `backend/app/services/speech_recognition_optimized.py` добавлено:

```python
# Отключение XET для избежания проблем с зависанием загрузок на Windows
os.environ["HF_HUB_ENABLE_HF_TRANSFER"] = "0"
```

Это отключает XET перед загрузкой моделей.

---

## 🚀 Рекомендация

**Самый быстрый способ:**
1. Перезапустите сервер (XET уже отключен в коде)
2. Если проблема остается - используйте Решение 2 (стандартный Whisper)

---

## 💡 Почему возникает проблема?

XET (eXtended Efficient Transfer) - это система для быстрой передачи больших файлов, но:
- ⚠️ Часто зависает на Windows
- ⚠️ Требует установки дополнительного пакета `hf_xet`
- ⚠️ Может иметь проблемы с сетевыми таймаутами

Отключение XET заставляет `huggingface_hub` использовать обычное HTTP скачивание, которое более стабильно на Windows.



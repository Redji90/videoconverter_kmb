# Решение проблем со скачиванием моделей

## 🔍 Почему модель не скачивается?

### Основные причины:

1. **Проблемы с интернет-соединением**
   - Таймауты
   - Медленная скорость
   - Нестабильное соединение

2. **Заблокированные файлы**
   - Другой процесс использует файлы
   - Незавершенная предыдущая загрузка

3. **Проблемы с HuggingFace серверами**
   - Временные проблемы
   - Высокая нагрузка

4. **Недостаточно места на диске**
   - Нужно ~2-3 GB свободного места

## ✅ Решения (по порядку)

### Решение 1: Очистить заблокированные файлы

Если есть `.incomplete` или `.lock` файлы:

```powershell
cd E:\whisper-models

# Закройте все процессы Python/приложения, которые могут использовать файлы

# Удалите заблокированные файлы вручную:
Remove-Item -Path "base\.cache" -Recurse -Force -ErrorAction SilentlyContinue

# Или переименуйте директорию и создайте новую:
Rename-Item -Path "base" -NewName "base_old" -ErrorAction SilentlyContinue
```

### Решение 2: Увеличить таймаут и повторить

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1

# Попробуйте снова
python download_faster_whisper_model.py base
```

Если прерывается - просто запустите снова, загрузка продолжит с места остановки.

### Решение 3: Проверить интернет и повторить

```powershell
# Проверьте интернет
ping huggingface.co

# Если работает, попробуйте снова
cd C:\prj\converter\backend
.\venv\Scripts\python.exe download_faster_whisper_model.py base
```

### Решение 4: Использовать стандартный Whisper (самое простое!)

Если скачивание не работает, используйте стандартный Whisper:

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1

# Удалить faster-whisper
pip uninstall faster-whisper -y

# Перезапустить сервер
.\run_server.ps1
```

**Преимущества:**
- ✅ Использует ваш `medium.pt` который уже есть
- ✅ Не нужно ничего скачивать
- ✅ Работает сразу

### Решение 5: Скачать через другой способ

Попробуйте использовать Python напрямую с увеличенным таймаутом:

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1

python -c "
from huggingface_hub import snapshot_download
import os

# Увеличиваем таймаут
os.environ['HF_HUB_DOWNLOAD_TIMEOUT'] = '300'

snapshot_download(
    repo_id='guillaumekln/faster-whisper-base',
    local_dir='E:/whisper-models/base',
    max_workers=1
)
"
```

## 🚀 Самый простой способ (рекомендуется)

Если скачивание не работает - **используйте стандартный Whisper**:

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1
pip uninstall faster-whisper -y
```

Тогда:
- ✅ Будет использоваться ваш `medium.pt`
- ✅ Ничего скачивать не нужно
- ✅ Работает сразу
- ⚠️ Немного медленнее, но работает!

## 📋 Чек-лист перед скачиванием

- [ ] Проверено интернет-соединение
- [ ] Есть свободное место на диске E: (минимум 2 GB)
- [ ] Закрыты все процессы, которые могут использовать файлы
- [ ] Удалены заблокированные файлы (.lock, .incomplete)
- [ ] Попробовано несколько раз (загрузка продолжается с места остановки)

## 💡 Альтернативные варианты

### Вариант 1: Использовать base модель стандартного Whisper

Модель base стандартного Whisper скачается автоматически при первом использовании (если выберете "base" в интерфейсе):

- Размер: ~150 MB
- Формат: .pt (стандартный)
- Скачивается автоматически
- Работает без Faster-Whisper

### Вариант 2: Скачать позже

Если проблемы временные - просто попробуйте позже. HuggingFace может иметь временные проблемы.

### Вариант 3: Использовать прокси (если есть)

Если используете прокси:

```powershell
$env:HTTP_PROXY="http://прокси:порт"
$env:HTTPS_PROXY="http://прокси:порт"
python download_faster_whisper_model.py base
```



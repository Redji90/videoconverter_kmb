# 🔧 Исправление: Whisper ищет модель в системном кэше

## 🎯 Проблема

Whisper ищет модель в системном кэше `C:\Users\Lenovo\.cache\whisper\medium.pt` вместо `E:\whisper-models\medium.pt`.

**Причина:** Whisper определяет путь к кэшу при импорте модуля, а не при вызове функции. Переменная `WHISPER_CACHE_DIR` должна быть установлена ДО импорта модуля `whisper`.

## ✅ Решение

Исправлено в коде:
1. **В `main.py`:** Переменная `WHISPER_CACHE_DIR` устанавливается ДО импорта сервисов
2. **В `speech_recognition_optimized.py`:** Переменная устанавливается ДО импорта `whisper`

## 🚀 Что делать

### Перезапустите сервер:

```powershell
cd C:\prj\converter\backend
.\run_server.ps1
```

При запуске вы должны увидеть:
```
✓ WHISPER_CACHE_DIR установлен: E:\whisper-models
✓ Модель medium.pt найдена (X.XX GB)
```

### После этого:

При конвертации видео Whisper должен использовать модель из `E:\whisper-models\medium.pt` и не скачивать её повторно.

---

## 🔍 Как проверить

После перезапуска сервера в логах должно быть:
```
✓ WHISPER_CACHE_DIR установлен: E:\whisper-models
✓ Модель medium.pt найдена (X.XX GB)
```

Если при конвертации появляется сообщение:
```
C:\Users\Lenovo\.cache\whisper\medium.pt exists, but the SHA256 checksum does not match
```

Это означает, что модель все еще ищется в системном кэше. В этом случае:

### Решение 1: Удалите модель из системного кэша

```powershell
Remove-Item "C:\Users\Lenovo\.cache\whisper\medium.pt" -ErrorAction SilentlyContinue
```

### Решение 2: Проверьте, что переменная установлена правильно

```powershell
# В PowerShell перед запуском сервера
$env:WHISPER_CACHE_DIR="E:\whisper-models"
cd C:\prj\converter\backend
.\run_server.ps1
```

---

## 📝 Технические детали

**Изменения:**

1. В `main.py`:
   - Переменная `WHISPER_CACHE_DIR` устанавливается из `config.py` ДО импорта сервисов
   - Добавлена проверка наличия модели

2. В `speech_recognition_optimized.py`:
   - Переменная устанавливается ДО импорта `whisper`
   - Используется значение из `config.py` или переменной окружения

---

## ✨ Результат

После перезапуска:
- ✅ Whisper будет использовать `E:\whisper-models\medium.pt`
- ✅ Модель не будет скачиваться повторно
- ✅ Не будет конфликтов с системным кэшем

**Перезапустите сервер и проверьте!**



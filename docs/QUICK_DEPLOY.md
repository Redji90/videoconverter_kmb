# ⚡ Быстрый деплой на Hugging Face Spaces

## 🚀 За 5 минут

### Шаг 1: Создайте Space

1. Откройте https://huggingface.co/new-space
2. Заполните:
   - **Space name**: `video-to-text-converter`
   - **SDK**: **Docker**
   - Нажмите **Create Space**

### Шаг 2: Загрузите файлы

**Вариант A: Через Git (рекомендуется)**

```bash
# В PowerShell
cd c:\prj\converter

# Инициализировать git если еще не инициализирован
git init

# Добавить remote (замените YOUR_USERNAME на ваш username)
git remote add origin https://huggingface.co/spaces/YOUR_USERNAME/video-to-text-converter

# Добавить файлы
git add Dockerfile README.md backend/ frontend/ .gitignore

# Закоммитить
git commit -m "Initial commit"

# Запушить (потребуется логин)
git push origin main
```

**Вариант B: Через веб-интерфейс**

1. В репозитории Space нажмите **Add file → Upload files**
2. Перетащите папки и файлы:
   - `Dockerfile`
   - `README.md`
   - Папка `backend/`
   - Папка `frontend/`

### Шаг 3: Дождитесь сборки

- Spaces автоматически начнет сборку
- Это займет 10-15 минут
- Следите за прогрессом в разделе **App logs**

### Шаг 4: Готово! 🎉

Откройте ваш Space: `https://huggingface.co/spaces/YOUR_USERNAME/video-to-text-converter`

---

## ✅ Что уже настроено

- ✅ Dockerfile для multi-stage build
- ✅ README.md с метаданными
- ✅ Поддержка статики (frontend)
- ✅ CORS настроен
- ✅ Пути для Docker окружения
- ✅ Health check

---

## 🔧 Дополнительные настройки

### Включить GPU

1. В Space перейдите в **Settings**
2. **Hardware**: Выберите **GPU** (T4 small)
3. Сохраните

### Добавить переменные окружения

1. **Settings → Repository secrets**
2. Добавьте `HF_TOKEN` (если нужна diarization)

---

## 🐛 Быстрое решение проблем

**Build fails?**
- Проверьте логи в **App logs**
- Убедитесь что все файлы загружены

**404 ошибка?**
- Проверьте что frontend собран (`npm run build`)
- Проверьте путь `/static` в логах

**Модели не загружаются?**
- Это нормально - модели загружаются при первом запросе
- Может занять несколько минут

---

## 📝 Проверка перед деплоем

- [ ] Dockerfile создан
- [ ] README.md с метаданными
- [ ] backend/ папка содержит все файлы
- [ ] frontend/ папка содержит package.json
- [ ] .gitignore создан (чтобы не загружать node_modules и venv)

---

Готово! Приложение должно заработать через 10-15 минут после пуша.



# Ручная загрузка моделей diarization через браузер

Если у вас проблемы с автоматической загрузкой через скрипт, можно скачать модели вручную через браузер.

## 📋 Модели для загрузки

Для работы diarization нужны две модели:

1. **pyannote/speaker-diarization-3.1** (основная модель)
2. **pyannote/segmentation-3.0** (зависимость)

## 🔐 Шаг 1: Примите условия использования

**ВНИМАНИЕ:** Перед скачиванием нужно принять условия использования!

1. Перейдите на страницу модели: https://huggingface.co/pyannote/speaker-diarization-3.1
2. Войдите в аккаунт HuggingFace (или создайте новый)
3. Нажмите кнопку **"Accept"** (принять условия)
4. Повторите для второй модели: https://huggingface.co/pyannote/segmentation-3.0

## 📥 Шаг 2: Скачайте модели

### Способ 1: Через интерфейс HuggingFace (рекомендуется)

#### Для pyannote/speaker-diarization-3.1:

1. Откройте: https://huggingface.co/pyannote/speaker-diarization-3.1
2. Перейдите на вкладку **"Files and versions"**
3. Найдите файл `config.yaml` и нажмите на него
4. Нажмите кнопку **"↓ Download"** справа
5. Повторите для всех файлов (обычно это `config.yaml`, `pytorch_model.bin` и другие файлы)

**Или используйте кнопку "↓" рядом с названием репозитория** - это скачает все файлы как архив.

#### Для pyannote/segmentation-3.0:

1. Откройте: https://huggingface.co/pyannote/segmentation-3.0
2. Перейдите на вкладку **"Files and versions"**
3. Скачайте все файлы аналогично

### Способ 2: Использование huggingface-cli (если установлен)

```powershell
cd backend
.\venv\Scripts\Activate.ps1

# Скачать модель diarization
huggingface-cli download pyannote/speaker-diarization-3.1 --local-dir "E:\huggingface-cache\hub\models--pyannote--speaker-diarization-3.1"

# Скачать модель segmentation
huggingface-cli download pyannote/segmentation-3.0 --local-dir "E:\huggingface-cache\hub\models--pyannote--segmentation-3.0"
```

## 📁 Шаг 3: Разместите файлы в правильной директории

HuggingFace кэширует модели в специальной структуре директорий. После скачивания нужно разместить файлы правильно:

### Структура директорий:

```
E:\huggingface-cache\
└── hub\
    ├── models--pyannote--speaker-diarization-3.1\
    │   └── snapshots\
    │       └── [хеш-commit]\
    │           ├── config.yaml
    │           ├── pytorch_model.bin (или model.bin)
    │           └── ... (другие файлы)
    └── models--pyannote--segmentation-3.0\
        └── snapshots\
            └── [хеш-commit]\
                ├── config.yaml
                ├── pytorch_model.bin
                └── ... (другие файлы)
```

### Автоматическое создание структуры:

Создайте скрипт для правильного размещения файлов:

```powershell
# Создайте директории
$basePath = "E:\huggingface-cache\hub"
New-Item -ItemType Directory -Path "$basePath\models--pyannote--speaker-diarization-3.1\snapshots\main" -Force
New-Item -ItemType Directory -Path "$basePath\models--pyannote--segmentation-3.0\snapshots\main" -Force

# После скачивания через браузер:
# 1. Распакуйте архив pyannote-speaker-diarization-3.1
# 2. Скопируйте все файлы в: E:\huggingface-cache\hub\models--pyannote--speaker-diarization-3.1\snapshots\main\
# 3. Распакуйте архив pyannote-segmentation-3.0  
# 4. Скопируйте все файлы в: E:\huggingface-cache\hub\models--pyannote--segmentation-3.0\snapshots\main\
```

## 🔍 Шаг 4: Проверка установки

После размещения файлов проверьте:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
python -c "from pyannote.audio import Pipeline; Pipeline.from_pretrained('pyannote/speaker-diarization-3.1', use_auth_token=None)"
```

Если ошибок нет - модель установлена правильно!

## ⚠️ Важные замечания

1. **Хеш commit**: HuggingFace использует хеш коммита в пути. Если вы не знаете хеш, можно:
   - Использовать `main` вместо хеша (не всегда работает)
   - Посмотреть хеш на странице модели в разделе "Files and versions"
   - Дать HuggingFace автоматически создать правильную структуру (запустить скрипт загрузки после ручного копирования)

2. **Символические ссылки**: HuggingFace использует символические ссылки. В Windows это может не работать, поэтому копируйте файлы напрямую.

3. **Размер моделей**: 
   - speaker-diarization-3.1: ~1.5 GB
   - segmentation-3.0: ~100-200 MB

## 🚀 Альтернативный способ: Использование Git

HuggingFace хранилища - это Git репозитории, можно клонировать:

```powershell
# Установите Git LFS (Large File Storage) - необходимо для больших файлов
# https://git-lfs.github.com/

# Клонируйте модели
cd E:\huggingface-cache\hub
git clone https://huggingface.co/pyannote/speaker-diarization-3.1 models--pyannote--speaker-diarization-3.1
git clone https://huggingface.co/pyannote/segmentation-3.0 models--pyannote--segmentation-3.0

# Но нужно будет правильно организовать структуру snapshots
```

## 💡 Рекомендация

Если у вас есть доступ к интернету и можно решить проблему с DNS/подключением, лучше использовать автоматический скрипт `download_diarization_model.py` - он правильно создаст всю структуру и разместит файлы.

Ручная загрузка рекомендуется только если:
- Есть проблемы с Python/скриптом
- Нужно скачать на другом компьютере и перенести
- Есть ограничения на использование Python для скачивания



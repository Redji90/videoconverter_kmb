# Настройка разделения по ролям (Diarization)

## Важно:

Модель diarization **автоматически скачается** при первом использовании опции "Разделение по ролям" в интерфейсе.

## Настройка пути на диск E:

### 1. Установите переменную окружения:

```powershell
# Для текущей сессии
$env:HF_HOME="E:\huggingface-cache"

# Или постоянная установка
[System.Environment]::SetEnvironmentVariable("HF_HOME", "E:\huggingface-cache", "User")
```

### 2. Создайте директорию:

```powershell
New-Item -ItemType Directory -Path "E:\huggingface-cache" -Force
```

### 3. Перезапустите сервер:

```powershell
cd C:\prj\converter\backend
.\run_server.ps1
```

## Использование:

1. Загрузите видео в интерфейсе
2. Отметьте опцию **"🎭 Разделение по ролям"**
3. (Опционально) Укажите количество спикеров
4. Нажмите "Конвертировать"

При первом использовании модель (~1.5 GB) автоматически скачается на диск E (если настроен `HF_HOME`).

## Проверка:

После перезапуска сервера должно быть:
```
✓ Модели HuggingFace (diarization) будут сохраняться в: E:\huggingface-cache
```

## Примечание:

Не нужно запускать отдельные скрипты для загрузки модели - она загрузится автоматически при первом использовании функции разделения по ролям.




# 🔧 Исправление: Whisper использует системный кэш вместо E:\whisper-models

## 🎯 Проблема

Whisper все еще ищет модель в системном кэше `C:\Users\Lenovo\.cache\whisper\medium.pt` и скачивает её повторно, даже если файл в системном кэше поврежден (SHA256 не совпадает).

**Сообщение об ошибке:**
```
C:\Users\Lenovo\.cache\whisper\medium.pt exists, but the SHA256 checksum does not match; re-downloading the file
```

## ✅ Решение

### Шаг 1: Удалить модель из системного кэша

```powershell
cd C:\prj\converter\backend
.\remove_system_cache.ps1
```

Или вручную:
```powershell
Remove-Item "C:\Users\Lenovo\.cache\whisper\medium.pt" -Force -ErrorAction SilentlyContinue
```

### Шаг 2: Перезапустить сервер

```powershell
.\run_server.ps1
```

Теперь `run_server.ps1` автоматически установит `WHISPER_CACHE_DIR="E:\whisper-models"` перед запуском Python.

---

## 🔍 Что было исправлено

1. **В `run_server.ps1`:**
   - Автоматически устанавливает `WHISPER_CACHE_DIR="E:\whisper-models"` если не установлен
   - Проверяет наличие модели в кэше перед запуском

2. **В `speech_recognition_optimized.py`:**
   - Улучшено логирование - показывает, где ищется модель
   - Проверяет наличие модели перед загрузкой

3. **Создан скрипт `remove_system_cache.ps1`:**
   - Удаляет модель из системного кэша
   - Помогает избежать конфликтов

---

## 💡 Почему это происходит

Whisper проверяет модель в системном кэше первым делом. Если файл существует, но поврежден (неправильный SHA256), он пытается скачать его заново. Удаление файла из системного кэша заставляет Whisper использовать `WHISPER_CACHE_DIR`.

---

## ✨ Результат

После выполнения шагов:
- ✅ Системный кэш очищен
- ✅ `WHISPER_CACHE_DIR` установлен автоматически
- ✅ Whisper будет использовать модель из `E:\whisper-models\medium.pt`
- ✅ Модель не будет скачиваться повторно

---

## 📝 Альтернативное решение

Если проблема все еще сохраняется, установите переменную окружения в системе:

```powershell
[System.Environment]::SetEnvironmentVariable("WHISPER_CACHE_DIR", "E:\whisper-models", "User")
```

Затем перезапустите PowerShell и сервер.

**Выполните шаги 1 и 2 выше - это должно решить проблему!**



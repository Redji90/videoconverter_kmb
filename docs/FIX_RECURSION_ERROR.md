# 🔧 Исправление ошибки рекурсии в torch.load

## 🎯 Проблема

Ошибка `maximum recursion depth exceeded` при использовании WhisperX diarization.

**Причина:** Патч для `torch.load` вызывал сам себя, создавая бесконечную рекурсию.

## ✅ Решение

Исправлен патч для `torch.load`:
- Сохранена оригинальная функция в глобальной переменной модуля
- Патч использует сохраненную функцию, а не `torch.load`

## 🚀 Что делать

**Перезапустите сервер:**

```powershell
cd C:\prj\converter\backend
.\run_server.ps1
```

После перезапуска diarization должна работать без ошибки рекурсии.

---

## 📝 Технические детали

**Изменения в `speech_recognition_optimized.py`:**
- Удален дубликат патча
- Добавлена глобальная переменная `_torch_load_original_global` для хранения оригинальной функции
- Патч использует эту переменную вместо `torch.load` для избежания рекурсии

---

## ✨ Результат

После перезапуска:
- ✅ Diarization работает через WhisperX
- ✅ Нет ошибки рекурсии
- ✅ Модель diarization уже загружена и готова к использованию

**Перезапустите сервер и попробуйте снова!**



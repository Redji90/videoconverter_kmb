# 🚀 Деплой на Hugging Face Spaces

## 📋 Пошаговая инструкция

### 1. Подготовка аккаунта

1. Зарегистрируйтесь на [Hugging Face](https://huggingface.co/join)
2. Создайте токен доступа:
   - Перейдите в [Settings → Access Tokens](https://huggingface.co/settings/tokens)
   - Создайте новый токен с правами **write**
   - Сохраните токен (понадобится для деплоя)

### 2. Создание Space

1. Перейдите на [https://huggingface.co/new-space](https://huggingface.co/new-space)
2. Заполните форму:
   - **Space name**: `video-to-text-converter` (или любое другое имя)
   - **SDK**: Выберите **Docker**
   - **License**: MIT (или другой)
   - **Visibility**: Public (или Private)

3. Нажмите **Create Space**

### 3. Подготовка репозитория

#### Вариант A: Через Git (Рекомендуется)

```bash
# Клонировать ваш Space репозиторий
git clone https://huggingface.co/spaces/YOUR_USERNAME/YOUR_SPACE_NAME
cd YOUR_SPACE_NAME

# Или если Space уже создан, добавьте remote
cd c:\prj\converter
git init
git remote add origin https://huggingface.co/spaces/YOUR_USERNAME/YOUR_SPACE_NAME

# Скопировать необходимые файлы
# (Dockerfile и README.md уже созданы в корне проекта)

# Закоммитить и запушить
git add Dockerfile README.md backend/ frontend/
git commit -m "Initial commit for Hugging Face Spaces"
git push origin main
```

#### Вариант B: Через веб-интерфейс

1. В репозитории Space нажмите **Add file → Upload files**
2. Загрузите файлы:
   - `Dockerfile` (из корня проекта)
   - `README.md` (обновленный с метаданными)
   - Папку `backend/`
   - Папку `frontend/`

### 4. Настройка переменных окружения

В репозитории Space:

1. Перейдите в **Settings → Repository secrets**
2. Добавьте секреты (если нужны):
   - `HF_TOKEN` - ваш HuggingFace токен (для diarization моделей)

Или добавьте в Dockerfile как ENV переменные.

### 5. Ожидание деплоя

1. После пуша Hugging Face автоматически начнет сборку
2. Проверьте статус в разделе **App logs**
3. Первая сборка может занять 10-15 минут

### 6. Проверка работы

После успешного деплоя:
- Откройте URL вашего Space: `https://huggingface.co/spaces/YOUR_USERNAME/YOUR_SPACE_NAME`
- Протестируйте загрузку видео
- Проверьте логи в разделе **App logs**

---

## 🔧 Оптимизации

### Оптимизация размера образа

Dockerfile уже использует multi-stage build для уменьшения размера.

### Оптимизация памяти

В `backend/config.py` можно изменить модель по умолчанию:

```python
DEFAULT_MODEL = "base"  # вместо "medium" для меньшего потребления RAM
```

### Использование GPU

Spaces автоматически предоставляет GPU, если:
- Выбран Hardware: **GPU** в настройках Space
- GPU доступен (до 48 часов в неделю на бесплатном тарифе)

Чтобы использовать GPU, добавьте в настройках Space:
1. **Settings → Hardware**: Выберите **GPU** (T4 small)

И в `backend/config.py`:
```python
USE_GPU = True  # или через переменную окружения
```

---

## 📝 Структура файлов для Spaces

```
your-space-repo/
├── Dockerfile          # Docker конфигурация
├── README.md          # Метаданные Space
├── backend/           # Backend код
│   ├── app/
│   │   └── main.py
│   ├── requirements.txt
│   └── config.py
└── frontend/          # Frontend код
    ├── src/
    ├── package.json
    └── vite.config.ts
```

---

## 🐛 Решение проблем

### Ошибка сборки

**Проблема**: Build fails с ошибкой памяти

**Решение**:
- Используйте более легкую модель (base вместо medium)
- Оптимизируйте Dockerfile (уже сделано)

### Модели не загружаются

**Проблема**: Модели Whisper не скачиваются

**Решение**:
- Проверьте доступность интернета во время сборки
- Модели загружаются при первом запросе, это нормально

### Frontend не отображается

**Проблема**: Белый экран или 404

**Решение**:
- Проверьте, что frontend собран (`npm run build`)
- Проверьте путь к статике в `app/main.py`
- Добавьте маршрут для статики (см. ниже)

### Ошибка CORS

**Проблема**: CORS ошибки при запросах

**Решение**:
- CORS уже настроен в `app/main.py` для всех источников
- Если проблема остается, проверьте настройки в Spaces

---

## 🔗 Добавление маршрута для статики

Если нужно обслуживать frontend через FastAPI, добавьте в `backend/app/main.py`:

```python
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

# После создания app
static_dir = os.path.join(os.path.dirname(__file__), "..", "static")
if os.path.exists(static_dir):
    app.mount("/static", StaticFiles(directory=static_dir), name="static")
    
    @app.get("/")
    async def read_root():
        return FileResponse(os.path.join(static_dir, "index.html"))
```

Но в текущей конфигурации frontend должен работать через прокси или напрямую.

---

## 📊 Мониторинг

- **Логи**: Доступны в разделе **App logs** Space
- **Метрики**: Spaces показывает использование CPU/GPU/RAM
- **Статус**: Зеленый индикатор при успешной работе

---

## 💡 Дополнительные улучшения

1. **Кэширование моделей**: Модели сохраняются в `/app/models` и переиспользуются
2. **Очередь задач**: Для длительных операций можно добавить Celery/Redis
3. **Ограничение размера файлов**: Настроить в FastAPI
4. **Rate limiting**: Защита от злоупотреблений

---

## 🔗 Полезные ссылки

- [Hugging Face Spaces Docs](https://huggingface.co/docs/hub/spaces)
- [Docker on Spaces](https://huggingface.co/docs/hub/spaces-sdks-docker)
- [Spaces Hardware](https://huggingface.co/docs/hub/spaces-gpus)



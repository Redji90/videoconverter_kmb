# Руководство по оптимизации и разделению по ролям

## ✅ Что добавлено:

### 1. Ускорение распознавания (4-5x быстрее)
- **Faster-Whisper** - оптимизированная версия Whisper
- Автоматическое определение GPU/CPU
- Режим скорости для быстрой обработки

### 2. Разделение по ролям (Speaker Diarization)
- Автоматическое определение спикеров
- Группировка текста по каждому спикеру
- Отображение меток спикеров в субтитрах

---

## 🚀 Быстрая установка:

### Шаг 1: Установите Faster-Whisper (для ускорения)
```powershell
cd C:\prj\converter\backend
.\venv\Scripts\python.exe -m pip install faster-whisper
```

### Шаг 2: (Опционально) Установите WhisperX (для разделения по ролям)
```powershell
.\venv\Scripts\python.exe -m pip install whisperx
```

### Шаг 3: Перезапустите backend
```powershell
# Остановите текущий сервер (Ctrl+C)
.\run_server.ps1
```

---

## 💻 Использование в интерфейсе:

### Ускорение:
1. Отметьте **"⚡ Режим скорости"** - обработка будет быстрее (но менее точно)
2. Выберите модель **"tiny"** или **"base"** для максимальной скорости

### Разделение по ролям:
1. Отметьте **"🎭 Разделение по ролям"**
2. (Опционально) Укажите количество спикеров, если знаете
3. Результат покажет текст, разделенный по каждому спикеру

---

## 📊 Ожидаемые результаты:

### Без оптимизаций:
- Скорость: 1x (базовая)
- Время для 10 минут видео: ~10-15 минут

### С Faster-Whisper:
- Скорость: 4-5x быстрее
- Время для 10 минут видео: ~2-3 минуты
- На GPU: 10-30x быстрее

### С разделением по ролям:
- Дополнительное время: +20-30%
- Результат: текст разделен по спикерам

---

## ⚙️ Дополнительные настройки:

### Использование GPU (если есть NVIDIA GPU):
```powershell
$env:USE_GPU="true"
.\run_server.ps1
```

### Проверка установки:
После перезапуска backend проверьте логи:
- ✓ "Используется: Faster-Whisper" - ускорение работает
- ✓ "Speaker Diarization: Доступен" - разделение по ролям работает

---

## 📝 Примеры использования:

### Быстрая обработка (без разделения по ролям):
- ✅ Режим скорости: ВКЛ
- ✅ Модель: base
- ❌ Разделение по ролям: ВЫКЛ

### Качественная обработка с разделением:
- ❌ Режим скорости: ВЫКЛ
- ✅ Модель: base или small
- ✅ Разделение по ролям: ВКЛ
- ✅ Количество спикеров: 2 (если знаете)

---

## 🔧 Устранение проблем:

### "Faster-Whisper не найден"
- Установите: `pip install faster-whisper`
- Перезапустите сервер

### "Speaker Diarization недоступен"
- Установите: `pip install whisperx`
- Может потребоваться токен HuggingFace (см. INSTALL_OPTIMIZATIONS.md)

### Медленная обработка
- Убедитесь, что Faster-Whisper установлен
- Используйте режим скорости
- Выберите модель "tiny" или "base"

---

Подробные инструкции: `backend/INSTALL_OPTIMIZATIONS.md`




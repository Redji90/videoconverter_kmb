# Сравнение моделей Whisper - какую выбрать?

## 📊 Размеры и характеристики моделей

| Модель | Размер | Скорость | Качество | RAM | Рекомендуется для |
|--------|--------|----------|----------|-----|-------------------|
| **tiny** | ~75 MB | ⭐⭐⭐⭐⭐ | ⭐⭐ | 1 GB | Тестирование, быстрые проверки |
| **base** | ~150 MB | ⭐⭐⭐⭐ | ⭐⭐⭐ | 1 GB | Базовое использование, быстрая обработка |
| **small** | ~500 MB | ⭐⭐⭐ | ⭐⭐⭐⭐ | 2 GB | Хороший баланс качества/скорости |
| **medium** | ~1.5 GB | ⭐⭐ | ⭐⭐⭐⭐⭐ | 5 GB | Высокое качество |
| **large** | ~3 GB | ⭐ | ⭐⭐⭐⭐⭐ | 10 GB | Максимальное качество |
| **large-v2** | ~3 GB | ⭐ | ⭐⭐⭐⭐⭐ | 10 GB | Улучшенная версия large |
| **large-v3** | ~3 GB | ⭐ | ⭐⭐⭐⭐⭐ | 10 GB | Самая последняя версия |

## 🎯 Рекомендации по выбору

### Если нужно быстро и с минимумом места:
**→ Используйте `base` или `small`**

```powershell
# В интерфейсе просто выберите "base" вместо "medium"
```

**Преимущества:**
- ✅ Маленький размер (150-500 MB)
- ✅ Быстрая загрузка
- ✅ Быстрая обработка
- ✅ Меньше потребление памяти

**Недостатки:**
- ⚠️ Немного меньше точность (но все равно хорошая)

### Если нужен баланс качества и размера:
**→ Используйте `small`**

- 500 MB (в 3 раза меньше чем medium)
- Хорошее качество
- Быстро скачивается

### Для максимального качества:
**→ Используйте `medium` или `large`**

Но учтите размер!

## 📥 Как скачать другие модели

### Base модель (рекомендуется для начала):

**Faster-Whisper:**
- Ссылка: https://huggingface.co/guillaumekln/faster-whisper-base
- Размер: ~150 MB
- Команда:
  ```powershell
  cd E:\whisper-models
  Remove-Item -Recurse -Force base -ErrorAction SilentlyContinue
  git clone https://huggingface.co/guillaumekln/faster-whisper-base base
  cd base
  git lfs pull
  ```

**Или через huggingface-cli:**
  ```powershell
  huggingface-cli download guillaumekln/faster-whisper-base --local-dir "E:\whisper-models\base"
  ```

### Small модель:

**Faster-Whisper:**
- Ссылка: https://huggingface.co/guillaumekln/faster-whisper-small
- Размер: ~500 MB
- Команда:
  ```powershell
  huggingface-cli download guillaumekln/faster-whisper-small --local-dir "E:\whisper-models\small"
  ```

## 🔄 Как изменить модель в приложении

### Вариант 1: В интерфейсе
Просто выберите другую модель в выпадающем списке:
- `tiny`
- `base` ← Рекомендуется, если medium не скачивается
- `small` ← Хороший компромисс
- `medium`
- `large`

### Вариант 2: По умолчанию

Измените в коде `backend/app/main.py`:

```python
model: str = Form("base"),  # Вместо "medium" или другое значение по умолчанию
```

## 💡 Что выбрать, если medium не скачивается?

### Вариант 1: Base (самый простой)
- ✅ 150 MB - скачается быстро
- ✅ Хорошее качество для большинства задач
- ✅ Быстрая обработка

### Вариант 2: Small (компромисс)
- ✅ 500 MB - разумный размер
- ✅ Лучше качество чем base
- ✅ Все еще быстро

### Вариант 3: Использовать стандартный Whisper
Если Faster-Whisper модели не скачиваются, можно использовать стандартный Whisper с `.pt` файлами:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip uninstall faster-whisper -y
```

Тогда будет использоваться `medium.pt` который у вас уже есть!

## 🎬 Пример использования

После скачивания `base` модели, просто в интерфейсе выберите "base" вместо "medium" - всё!

## ⚡ Быстрая команда для base модели

```powershell
cd backend
.\venv\Scripts\Activate.ps1
huggingface-cli download guillaumekln/faster-whisper-base --local-dir "E:\whisper-models\base"
```

Это займет намного меньше времени чем medium (150 MB vs 1.5 GB)!



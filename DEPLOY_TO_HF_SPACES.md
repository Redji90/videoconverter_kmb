# 🚀 Развертывание на Hugging Face Spaces

Это руководство поможет вам развернуть проект на Hugging Face Spaces.

## 📋 Предварительные требования

1. Аккаунт на [Hugging Face](https://huggingface.co/)
2. Установленный Git
3. Установленный [Hugging Face CLI](https://huggingface.co/docs/huggingface_hub/guides/cli) (опционально)

## 🔧 Шаги развертывания

### 1. Клонирование репозитория Spaces

```bash
# Клонируйте ваш Space репозиторий
git clone https://huggingface.co/spaces/Vladislava11/videoconverter
cd videoconverter
```

### 2. Копирование файлов проекта

Скопируйте следующие файлы и папки из вашего локального проекта:

```bash
# Из корня проекта converter/
cp Dockerfile videoconverter/
cp .dockerignore videoconverter/
cp README_HF_SPACES.md videoconverter/README.md
cp -r backend videoconverter/
cp -r frontend videoconverter/
```

### 3. Структура файлов в Space

Убедитесь, что структура выглядит так:

```
videoconverter/
├── Dockerfile
├── .dockerignore
├── README.md (из README_HF_SPACES.md)
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   └── services/
│   ├── config.py
│   └── requirements.txt
└── frontend/
    ├── src/
    ├── package.json
    └── vite.config.ts
```

### 4. Коммит и пуш

```bash
cd videoconverter

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Add Video to Text Converter application"

# Отправьте на Spaces
git push
```

### 5. Ожидание сборки

После `git push` Hugging Face Spaces автоматически:
1. Соберет Docker образ
2. Установит все зависимости
3. Соберет frontend
4. Запустит приложение на порту 7860

Это может занять 5-15 минут при первой сборке.

## 🔍 Проверка статуса

1. Откройте [ваш Space](https://huggingface.co/spaces/Vladislava11/videoconverter)
2. Проверьте вкладку "Logs" для просмотра процесса сборки
3. После успешной сборки приложение будет доступно

## ⚠️ Важные замечания

### Модели Whisper

- Модели будут автоматически загружены при первом использовании
- Это может занять время при первом запросе
- Рекомендуется использовать модель `base` для баланса скорости и точности

### Ограничения Spaces

- **CPU**: Доступен CPU с ограниченными ресурсами
- **Память**: Ограниченная память (обычно 16GB)
- **Время выполнения**: Запросы могут быть прерваны через 60 секунд (для бесплатных Spaces)
- **Хранилище**: Модели кэшируются в `/app/models` и `/app/huggingface-cache`

### Оптимизация

Для ускорения работы:
1. Используйте модель `base` или `small` вместо `large`
2. Обрабатывайте короткие видео (< 5 минут)
3. Используйте `faster-whisper` (уже включен в requirements.txt)

## 🐛 Решение проблем

### Ошибка сборки

1. Проверьте логи в разделе "Logs" вашего Space
2. Убедитесь, что все файлы скопированы правильно
3. Проверьте, что Dockerfile использует порт 7860

### Модели не загружаются

1. Проверьте логи приложения
2. Убедитесь, что есть доступ к интернету (для загрузки моделей)
3. Проверьте переменные окружения `WHISPER_CACHE_DIR` и `HF_HOME`

### Frontend не отображается

1. Проверьте, что frontend собран (`npm run build`)
2. Убедитесь, что статические файлы скопированы в `/app/static`
3. Проверьте логи для ошибок

## 📚 Дополнительные ресурсы

- [Документация Hugging Face Spaces](https://huggingface.co/docs/hub/spaces)
- [Docker Spaces Guide](https://huggingface.co/docs/hub/spaces-sdks-docker)
- [Основной README проекта](README.md)

## 🔄 Обновление приложения

Для обновления приложения:

```bash
cd videoconverter

# Получите последние изменения из основного репозитория
# (скопируйте обновленные файлы)

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Update application"

# Отправьте изменения
git push
```

Spaces автоматически пересоберет и перезапустит приложение.


# Простое решение: Использовать стандартный Whisper

Если модели Faster-Whisper не скачиваются, можно использовать **стандартный Whisper** с моделью `medium.pt`, которая у вас уже есть!

## 🚀 Быстрое решение

### Вариант 1: Отключить Faster-Whisper (использовать ваш medium.pt)

```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1

# Удалить faster-whisper
pip uninstall faster-whisper -y

# Перезапустить сервер
.\run_server.ps1
```

После этого:
- ✅ Будет использоваться стандартный Whisper
- ✅ Модель `medium.pt` которая у вас уже есть
- ✅ Ничего скачивать не нужно!
- ⚠️ Будет немного медленнее, но работает сразу

### Вариант 2: Скачать base модель стандартного Whisper (если нужна)

Если нужна модель base:

```powershell
# Модель base стандартного Whisper скачивается автоматически при первом использовании
# Просто выберите "base" в интерфейсе - она скачается в формате .pt
```

## 💡 Рекомендация

**Самый простой способ:**
1. Удалите faster-whisper: `pip uninstall faster-whisper -y`
2. Перезапустите сервер
3. Используйте ваш `medium.pt` - всё работает!

**Преимущества:**
- ✅ Не нужно ничего скачивать
- ✅ Работает сразу
- ✅ Использует модель, которая у вас уже есть

**Недостатки:**
- ⚠️ Немного медленнее (но разница не критична)
- ⚠️ Больше потребление памяти

## 🔄 Если хотите вернуть Faster-Whisper позже

Можно установить обратно:
```powershell
pip install faster-whisper
```

И тогда модели будут скачиваться автоматически при использовании.



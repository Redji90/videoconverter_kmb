# Следующие шаги - Что делать сейчас

## ✅ Что уже готово:
- ✅ Backend код создан
- ✅ Frontend код создан
- ✅ Виртуальное окружение создано
- ✅ Python зависимости установлены
- ✅ Настроен путь для моделей Whisper

## 📋 Что нужно сделать сейчас:

### Шаг 1: Установить Frontend зависимости

Откройте PowerShell и выполните:

```powershell
cd C:\prj\converter\frontend
npm install
```

Это установит все необходимые пакеты для React приложения.

---

### Шаг 2: (Опционально) Настроить путь для моделей Whisper на диск E

Если хотите сохранять модели на диск E:

```powershell
# Для текущей сессии
$env:WHISPER_CACHE_DIR="E:\whisper-models"

# Или для постоянной установки
[System.Environment]::SetEnvironmentVariable("WHISPER_CACHE_DIR", "E:\whisper-models", "User")
```

---

### Шаг 3: Запустить Backend сервер

Откройте **первый терминал PowerShell**:

```powershell
cd C:\prj\converter\backend

# Вариант 1: Использовать скрипт (рекомендуется)
.\run_server.ps1

# Вариант 2: Вручную
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

**Оставьте этот терминал открытым!**

---

### Шаг 4: Запустить Frontend

Откройте **второй терминал PowerShell** (новое окно):

```powershell
cd C:\prj\converter\frontend
npm run dev
```

Вы должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

**Оставьте этот терминал открытым!**

---

### Шаг 5: Открыть приложение в браузере

Откройте браузер и перейдите по адресу:
```
http://localhost:5173
```

---

### Шаг 6: Протестировать приложение

1. **Загрузите видео файл:**
   - Нажмите на область загрузки или перетащите файл
   - Поддерживаются форматы: MP4, AVI, MOV, MKV и др.

2. **Выберите настройки:**
   - Язык: выберите язык или "Автоопределение"
   - Модель: начните с "Base" (рекомендуется)

3. **Нажмите "Конвертировать в текст"**

4. **Дождитесь результата:**
   - При первом использовании модель будет скачана (может занять время)
   - После обработки увидите текст и временные метки

---

## 🔧 Проверка установки FFmpeg

Убедитесь, что FFmpeg установлен:

```powershell
ffmpeg -version
```

Если FFmpeg не найден:
- Установите через Chocolatey: `choco install ffmpeg`
- Или скачайте с https://ffmpeg.org/download.html
- Добавьте в PATH

---

## ⚠️ Возможные проблемы

### Ошибка при запуске backend:
- Убедитесь, что виртуальное окружение создано: `Test-Path backend\venv`
- Проверьте установку зависимостей: `.\venv\Scripts\python.exe -m pip list`

### Ошибка при запуске frontend:
- Убедитесь, что Node.js установлен: `node --version`
- Переустановите зависимости: `npm install`

### Медленная обработка:
- Это нормально при первом запуске (скачивается модель)
- Используйте модель "tiny" для быстрой проверки

---

## 📝 Краткая шпаргалка команд

```powershell
# Backend (Терминал 1)
cd C:\prj\converter\backend
.\run_server.ps1

# Frontend (Терминал 2)
cd C:\prj\converter\frontend
npm run dev

# Браузер
http://localhost:5173
```

---

## 🎯 Готово!

После выполнения всех шагов у вас будет работающее приложение для конвертации видео в текст!




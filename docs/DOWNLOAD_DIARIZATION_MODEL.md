# Как скачать модель diarization (model.bin) отдельно

Модель для разделения по ролям (diarization) автоматически загружается при первом использовании функции "Разделение по ролям". Но вы можете загрузить её заранее, чтобы не ждать во время работы.

## 🚀 Быстрый способ (PowerShell)

```powershell
cd backend
.\download_diarization.ps1
```

## 📋 Пошаговая инструкция

### Вариант 1: Использование PowerShell скрипта (рекомендуется)

1. Откройте PowerShell в директории `backend`
2. Убедитесь, что виртуальное окружение создано
3. Запустите скрипт:

```powershell
cd C:\prj\converter\backend
.\download_diarization.ps1
```

Скрипт автоматически:
- Установит путь `HF_HOME=E:\huggingface-cache` (или использует из окружения)
- Создаст необходимую директорию
- Загрузит модель через WhisperX

### Вариант 2: Использование Python скрипта напрямую

1. Активируйте виртуальное окружение:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
```

2. (Опционально) Установите путь для HuggingFace:

```powershell
$env:HF_HOME="E:\huggingface-cache"
```

3. Запустите скрипт:

```powershell
python download_diarization_model.py
```

### Вариант 3: Через Python с явным указанием пути

```powershell
cd backend
.\venv\Scripts\Activate.ps1
$env:HF_HOME="E:\huggingface-cache"
python download_diarization_model.py
```

## ✅ Проверка загрузки

После успешной загрузки вы увидите:

```
============================================================
✓ Модель diarization успешно загружена!
✓ Сохранена в: E:\huggingface-cache
============================================================

Теперь можно использовать разделение по ролям в приложении!
Модель больше не будет скачиваться при использовании.
```

## 📁 Где сохраняется модель

Модель сохраняется в директории, указанной в `HF_HOME`:

- По умолчанию: `E:\huggingface-cache`
- Структура: `E:\huggingface-cache\hub\models--pyannote--speaker-diarization-3.1\...`
- Файл модели: `model.bin` (~145MB, общий размер модели ~1.5GB с дополнительными файлами)

## 🔍 Проверка, что модель загружена

После загрузки проверьте содержимое директории:

```powershell
# Проверка наличия модели
Get-ChildItem -Path "E:\huggingface-cache" -Recurse -Filter "model.bin" | Select-Object FullName
```

Или используйте скрипт проверки:

```powershell
python check_model_paths.py
```

## ⚠️ Требования

- Виртуальное окружение должно быть создано и активировано
- Установлен `whisperx`: `pip install whisperx`
- Настроен путь `HF_HOME` (или будет использован по умолчанию `E:\huggingface-cache`)
- Достаточно места на диске (~1.5 GB)
- Активное интернет-соединение
- **ВАЖНО:** Модель требует авторизацию HuggingFace (см. ниже)

## 🔐 Авторизация HuggingFace (ОБЯЗАТЕЛЬНО)

Модель `pyannote/speaker-diarization-3.1` является защищенной и требует:

### Шаг 1: Примите условия использования

Модель diarization использует несколько компонентов, для которых нужно принять условия:

1. **pyannote/speaker-diarization-3.1** (основная модель)
   - Перейдите: https://huggingface.co/pyannote/speaker-diarization-3.1
   - Войдите в аккаунт HuggingFace
   - Нажмите кнопку **"Accept"**

2. **pyannote/segmentation-3.0** (зависимость, также требует авторизацию)
   - Перейдите: https://huggingface.co/pyannote/segmentation-3.0
   - Войдите в аккаунт HuggingFace
   - Нажмите кнопку **"Accept"**

**Важно:** Нужно принять условия для **обеих** моделей!

### Шаг 2: Получите токен доступа

1. Перейдите в настройки токенов: https://huggingface.co/settings/tokens
2. Нажмите **"New token"** (новый токен)
3. Введите имя токена (например, "diarization-model")
4. Выберите тип **"Read"** (достаточно прав на чтение)
5. Нажмите **"Generate token"**
6. **Скопируйте токен** (он показывается только один раз!)

### Шаг 3: Установите токен

**Временная установка (для текущей сессии):**
```powershell
$env:HF_TOKEN="ваш_токен_здесь"
```

**Постоянная установка (рекомендуется):**
```powershell
[System.Environment]::SetEnvironmentVariable("HF_TOKEN", "ваш_токен_здесь", "User")
```

После этого перезапустите PowerShell.

### Шаг 4: Запустите скрипт загрузки

```powershell
cd backend
.\download_diarization.ps1
```

**Или через Python:**
```powershell
cd backend
.\venv\Scripts\Activate.ps1
python download_diarization_model.py
```

## 🔧 Установка зависимостей

Если скрипт выдает ошибку о отсутствующих библиотеках:

```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install whisperx pyannote.audio
```

Или установите все зависимости:

```powershell
pip install -r requirements.txt
```

## 📥 Альтернативный способ: Ручная загрузка через браузер

Если автоматическая загрузка не работает, можно скачать модели вручную:

**См. подробную инструкцию:** `backend/MANUAL_MODEL_DOWNLOAD.md`

Кратко:
1. Примите условия использования моделей на HuggingFace
2. Скачайте модели через браузер со страниц моделей
3. Разместите файлы в правильной структуре директорий

## 💡 Что делать, если загрузка не удалась

### Ошибка авторизации

Если видите ошибку: `"Could not download 'pyannote/speaker-diarization-3.1' pipeline"`

**Решение:**
1. Убедитесь, что вы приняли условия использования на HuggingFace
2. Проверьте, что токен установлен: `echo $env:HF_TOKEN`
3. Убедитесь, что токен правильный (без лишних пробелов)
4. Попробуйте создать новый токен

### Другие проблемы

1. **Проверьте интернет-соединение** - модель скачивается с HuggingFace
2. **Проверьте доступное место** - нужно ~1.5 GB свободного места
3. **Проверьте путь** - убедитесь, что `HF_HOME` указывает на существующий диск
4. **Проверьте версию whisperx** - попробуйте обновить: `pip install --upgrade whisperx`
5. **Попробуйте загрузить вручную** - модель загрузится автоматически при первом использовании (но токен все равно потребуется)

## 📝 Примечания

- Модель загружается **один раз** - после загрузки она сохраняется локально
- Если модель уже загружена, скрипт просто проверит её наличие
- Размер модели: ~1.5 GB (с дополнительными файлами конфигурации)
- Время загрузки зависит от скорости интернета (обычно 5-15 минут)

## 🎯 После загрузки

После успешной загрузки вы можете использовать разделение по ролям в приложении:

1. Запустите бэкенд: `.\run_server.ps1`
2. Запустите фронтенд: `npm run dev`
3. Загрузите видео
4. Включите опцию **"🎭 Разделение по ролям"**
5. Модель будет использована сразу, без дополнительной загрузки!


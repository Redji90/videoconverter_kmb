# ⚡ Ускорение и разделение по спикерам

## 🎯 Ваши потребности

1. **Разбивка по спикерам** (diarization)
2. **Ускорение** (сейчас 6.6 минут - слишком долго)

## ✅ Решение 1: Включить разделение по спикерам

### В интерфейсе:

1. Загрузите видео
2. **Включите опцию "🎭 Разделение по ролям"** (галочка)
3. Выберите модель
4. Нажмите "Конвертировать"

### Результат:

Текст будет разделен по спикерам:
- `SPEAKER_00: текст первого спикера`
- `SPEAKER_01: текст второго спикера`
и т.д.

---

## ⚡ Решение 2: Ускорение транскрипции

### Вариант A: Использовать модель меньшего размера (БЫСТРО)

**В интерфейсе выберите модель: "base" вместо "medium"**

| Модель | Скорость | Качество | Размер |
|--------|----------|----------|--------|
| **tiny** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ~39 MB |
| **base** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ~74 MB |
| **small** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ~244 MB |
| **medium** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ~1.4 GB |
| **large** | ⭐ | ⭐⭐⭐⭐⭐ | ~3 GB |

**Рекомендация:** Используйте **"base"** - хороший баланс скорости и качества.

**Ожидаемое ускорение:** 2-3x быстрее (вместо 6.6 минут → ~2-3 минуты)

---

### Вариант B: Оптимизировать параметры

В интерфейсе (если доступно) или через API можно установить:
- `beam_size=1` вместо 5 (быстрее, но менее точно)
- `best_of=1` вместо 5 (быстрее)

**Ожидаемое ускорение:** 2-4x быстрее

---

### Вариант C: Попробовать Faster-Whisper (БОЛЬШОЕ УСКОРЕНИЕ)

Faster-Whisper может быть в 4-5x быстрее стандартного Whisper.

**Установка:**
```powershell
cd C:\prj\converter\backend
.\venv\Scripts\Activate.ps1
pip install faster-whisper
```

**Проблема:** Ранее были проблемы с XET при скачивании моделей.

**Решение:**
1. XET уже отключен в коде
2. Попробуйте установить faster-whisper снова
3. Если модели не скачиваются - используйте вариант A (base модель)

**Ожидаемое ускорение:** 4-5x быстрее (вместо 6.6 минут → ~1.5-2 минуты)

---

### Вариант D: Использовать GPU (если доступен)

Если у вас есть NVIDIA GPU с CUDA:

1. Установите CUDA-версию PyTorch:
```powershell
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

2. Установите Faster-Whisper:
```powershell
pip install faster-whisper
```

3. В `backend/config.py` или переменной окружения:
```powershell
$env:USE_GPU="true"
```

**Ожидаемое ускорение:** 10-30x быстрее на GPU!

---

## 📊 Сравнение вариантов

| Вариант | Сложность | Ускорение | Качество |
|---------|-----------|-----------|----------|
| **A: Модель base** | ⭐ Легко | 2-3x | ⭐⭐⭐ |
| **B: Параметры** | ⭐ Легко | 2-4x | ⭐⭐⭐ |
| **C: Faster-Whisper** | ⭐⭐ Средне | 4-5x | ⭐⭐⭐⭐⭐ |
| **D: GPU** | ⭐⭐⭐ Сложно | 10-30x | ⭐⭐⭐⭐⭐ |

---

## 🚀 Рекомендация

### Для быстрого результата (СЕЙЧАС):

1. **Включите "Разделение по ролям"** в интерфейсе
2. **Используйте модель "base"** вместо "medium"
   - Скорость: ~2-3 минуты вместо 6.6 минут
   - Качество: хорошее для большинства случаев

### Для максимальной скорости (В БУДУЩЕМ):

1. Попробуйте установить Faster-Whisper:
   ```powershell
   pip install faster-whisper
   ```
2. Если скачивание моделей работает - используйте его
3. Если нет - используйте вариант A (base модель)

---

## 📝 Настройка в интерфейсе

В интерфейсе вы можете:
1. Выбрать модель: **"base"** (быстрее) или **"medium"** (лучше качество)
2. Включить **"🎭 Разделение по ролям"** (галочка)
3. При необходимости настроить другие параметры

---

## ✨ Итог

**Для получения разбивки по спикерам:**
- ✅ Включите опцию "Разделение по ролям" в интерфейсе

**Для ускорения:**
- ✅ Используйте модель **"base"** вместо "medium" (2-3x быстрее)
- ✅ Или попробуйте установить Faster-Whisper (4-5x быстрее)

**Попробуйте прямо сейчас:**
1. Загрузите видео
2. Выберите модель: **"base"**
3. Включите **"Разделение по ролям"**
4. Конвертируйте!



# Альтернативные модели для Diarization (разделение по ролям)

## 📋 Текущая модель в проекте

**pyannote/speaker-diarization-3.1** (рекомендуется)
- Качество: ⭐⭐⭐⭐⭐
- Размер: ~1.5 GB
- Требует: Авторизацию HuggingFace
- Зависимости: pyannote/segmentation-3.0

## 🔄 Альтернативные модели Pyannote

### 1. pyannote/speaker-diarization-3.0 (старая версия)
- URL: https://huggingface.co/pyannote/speaker-diarization-3.0
- Размер: ~1.5 GB
- Стабильная, но более старая версия
- Может не требовать авторизацию (проверьте)

### 2. pyannote/speaker-diarization-2.1
- URL: https://huggingface.co/pyannote/speaker-diarization-2.1
- Размер: ~1.2 GB
- Еще более старая версия
- Обычно не требует авторизацию

### 3. pyannote/speaker-diarization
- Базовая версия (без номера версии)
- Самый простой вариант
- Может быть менее точной

## 🆕 Альтернативные библиотеки и модели

### 1. Resemblyzer (более легкая альтернатива) ⭐ РЕКОМЕНДУЕТСЯ

**Установка:**
```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install resemblyzer librosa soundfile
```

**Использование:**
```python
from resemblyzer import VoiceEncoder, preprocess_wav
encoder = VoiceEncoder()
wav = preprocess_wav("audio.wav")
embed = encoder.embed_utterance(wav)
```

**Плюсы:**
- ✅ Легковесная (~50 MB)
- ✅ Быстрая
- ✅ Не требует авторизацию HuggingFace
- ✅ Не требует segmentation-3.0
- ✅ Простая в использовании
- ✅ Работает сразу после установки

**Минусы:**
- ❌ Меньше точность (чем Pyannote)
- ❌ Может хуже работать с шумом
- ❌ Не так хорошо поддерживается
- ❌ Нет встроенной diarization (нужна кластеризация)

**Где скачать:** 
- Установка: `pip install resemblyzer`
- GitHub: https://github.com/resemble-ai/Resemblyzer
- **Подробная инструкция:** `backend/RESEMBLYZER_SETUP.md`

### 2. NeMo Speaker Diarization (от NVIDIA)
```python
# Установка
pip install nemo_toolkit[all]

# Использование
import nemo.collections.asr as nemo_asr
```

**Плюсы:**
- ✅ Очень точная
- ✅ Хорошая документация
- ✅ Поддержка GPU (NVIDIA)
- ✅ Активно поддерживается

**Минусы:**
- ❌ Большой размер
- ❌ Требует больше ресурсов
- ❌ Сложнее в настройке

**Где скачать:** https://github.com/NVIDIA/NeMo

### 3. SpeechBrain (от Facebook)
```python
# Установка
pip install speechbrain

# Использование
from speechbrain.inference.speaker import SpeakerRecognition
```

**Плюсы:**
- ✅ Разные модели на выбор
- ✅ Хорошая точность
- ✅ Активная разработка
- ✅ Много примеров

**Минусы:**
- ❌ Может быть сложно настроить
- ❌ Требует понимания архитектуры

**Где скачать:** https://github.com/speechbrain/speechbrain

### 4. WavLM / Wav2Vec2 (от Facebook)
- Модели для извлечения признаков
- Можно использовать для diarization с дополнительной обработкой
- Хорошая точность, но требует больше работы

### 5. AssemblyAI (платный API)
```python
# Установка
pip install assemblyai

# Использование
import assemblyai as aai
aai.settings.api_key = "YOUR_API_KEY"
transcriber = aai.Transcriber()
```

**Плюсы:**
- ✅ Отличное качество
- ✅ Готовая интеграция
- ✅ Не требует локальных ресурсов
- ✅ Поддержка многих языков

**Минусы:**
- ❌ Платный сервис
- ❌ Требует интернет
- ❌ Зависимость от внешнего сервиса

**Цены:** от $0.00025/минута

### 6. Deepgram (платный API)
- Аналогично AssemblyAI
- Хорошее качество
- Разные тарифы

### 7. Azure Speech Services (платный)
- От Microsoft
- Очень качественный
- Интеграция с Azure экосистемой

## 📊 Сравнение моделей

| Модель | Точность | Скорость | Размер | Требует авторизацию | Сложность |
|--------|----------|----------|--------|---------------------|-----------|
| **pyannote-3.1** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ~1.5 GB | ✅ Да | Средняя |
| **pyannote-3.0** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ~1.5 GB | ⚠️ Возможно | Средняя |
| **pyannote-2.1** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ~1.2 GB | ❌ Нет | Средняя |
| **Resemblyzer** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ~50 MB | ❌ Нет | Низкая |
| **NeMo** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ~2 GB | ❌ Нет | Высокая |
| **SpeechBrain** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ~1 GB | ❌ Нет | Средняя |
| **AssemblyAI** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | - | ✅ API ключ | Низкая |

## 🔧 Как использовать альтернативную модель

### Вариант 1: Использовать другую версию Pyannote

В `backend/app/services/speech_recognition_optimized.py` можно изменить:

```python
# Вместо speaker-diarization-3.1 использовать другую версию
diarize_model = whisperx.DiarizationPipeline(
    use_auth_token=hf_token,
    device=device,
    model_name="pyannote/speaker-diarization-2.1"  # или другая версия
)
```

**Примечание:** WhisperX может не поддерживать все версии напрямую. Возможно, потребуется использовать pyannote напрямую.

### Вариант 2: Использовать pyannote напрямую (без WhisperX)

```python
from pyannote.audio import Pipeline

# Используйте другую версию модели
pipeline = Pipeline.from_pretrained(
    "pyannote/speaker-diarization-2.1",  # или другая версия
    use_auth_token=None  # может не требоваться для старых версий
)
```

### Вариант 3: Использовать Resemblyzer (легковесная альтернатива)

```python
# Установите
pip install resemblyzer

# Используйте в коде
from resemblyzer import VoiceEncoder
encoder = VoiceEncoder()
```

## 💡 Рекомендации

### Для начала (если проблемы с авторизацией):
- Попробуйте **pyannote/speaker-diarization-2.1** или **3.0** - могут не требовать авторизацию

### Для максимальной точности:
- Используйте **pyannote/speaker-diarization-3.1** (текущая)
- Или **NeMo** (но сложнее)

### Для быстроты и простоты:
- Используйте **Resemblyzer**
- Или **AssemblyAI API** (если готовы платить)

### Для минимальных ресурсов:
- Используйте **Resemblyzer** (~50 MB)
- Или **pyannote-2.1** (~1.2 GB, но проще)

## 📝 Где скачать модели

### Pyannote модели:
- https://huggingface.co/pyannote - все модели pyannote
- https://huggingface.co/pyannote/speaker-diarization-3.1 - текущая
- https://huggingface.co/pyannote/speaker-diarization-2.1 - альтернатива

### Другие библиотеки:
- Resemblyzer: `pip install resemblyzer`
- NeMo: `pip install nemo_toolkit[all]`
- SpeechBrain: `pip install speechbrain`

## 🔍 Проверка доступности моделей

Чтобы проверить, какие модели доступны без авторизации:

```python
from pyannote.audio import Pipeline

# Попробуйте загрузить без токена
try:
    pipeline = Pipeline.from_pretrained(
        "pyannote/speaker-diarization-2.1",
        use_auth_token=None
    )
    print("✅ Модель доступна без авторизации")
except Exception as e:
    print(f"❌ Требуется авторизация: {e}")
```

## 🚀 Быстрое переключение на альтернативу

Если хотите попробовать другую модель, можно создать скрипт:

```python
# test_alternative_model.py
from pyannote.audio import Pipeline

# Попробуйте разные модели
models_to_try = [
    "pyannote/speaker-diarization-2.1",
    "pyannote/speaker-diarization-3.0",
    "pyannote/speaker-diarization-3.1"
]

for model_name in models_to_try:
    try:
        print(f"Пробуем {model_name}...")
        pipeline = Pipeline.from_pretrained(model_name, use_auth_token=None)
        print(f"✅ {model_name} - работает без авторизации!")
        break
    except Exception as e:
        print(f"❌ {model_name} - ошибка: {str(e)[:100]}")
```


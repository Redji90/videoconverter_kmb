# 🔧 Исправление ошибки 500 при diarization

## 🎯 Проблема

При использовании разделения по ролям возникает ошибка 500:
- WhisperX не может загрузить модель (проблема с HuggingFace Hub)
- Система переключается на простую diarization
- Но затем происходит ошибка 500

## ✅ Что исправлено

1. **Улучшена обработка ошибок** - теперь все ошибки выводятся в консоль
2. **Исправлено создание объекта `info`** для стандартного Whisper
3. **Добавлена проверка на пустые сегменты**
4. **Улучшено логирование** - теперь видно, где именно происходит ошибка

## 🚀 Что делать сейчас

### Шаг 1: Перезапустите сервер

```powershell
cd C:\prj\converter\backend
.\run_server.ps1
```

### Шаг 2: Попробуйте снова

Включите "Разделение по ролям" в интерфейсе и попробуйте конвертировать видео.

### Шаг 3: Проверьте логи

Теперь в консоли сервера будут подробные сообщения об ошибках, если что-то пойдет не так.

---

## 🔍 Диагностика

Если ошибка 500 все еще возникает, проверьте логи сервера. Вы должны увидеть сообщения:

```
⚠️  WhisperX diarization не удалось: ...
   Используем простую diarization на основе пауз
```

Или:

```
❌ Ошибка при транскрипции: ...
```

Скопируйте полный текст ошибки из консоли сервера.

---

## 💡 Альтернативное решение

Если проблема сохраняется, можно временно отключить diarization и использовать обычную транскрипцию:

1. **В интерфейсе** - не включайте опцию "Разделение по ролям"
2. Получите обычный текст без разделения по спикерам
3. Diarization можно настроить позже

---

## 📝 Технические детали

**Что было изменено:**

1. В `speech_recognition_optimized.py`:
   - Улучшена обработка исключений при fallback на простую diarization
   - Исправлено создание объекта `info` для совместимости
   - Добавлена проверка на пустые сегменты

2. В `main.py`:
   - Добавлен подробный вывод ошибок при транскрипции
   - Улучшена обработка исключений с traceback

---

## ✨ Результат

Теперь система должна:
- ✅ Правильно переключаться на простую diarization при ошибке WhisperX
- ✅ Показывать подробные сообщения об ошибках
- ✅ Продолжать работу даже если diarization не удался

**Перезапустите сервер и попробуйте снова!**



# 🔧 XET снова блокирует загрузку Faster-Whisper

## 🎯 Проблема

XET пытается скачать модель Faster-Whisper и зависает:
```
Xet Storage is enabled for this repo, but the 'hf_xet' package is not installed.
model.bin: 0%|...| 0.00/145M
```

## ✅ Решение 1: Остановить и использовать стандартный Whisper с оптимизацией

### Шаг 1: Остановите процесс (Ctrl+C)

### Шаг 2: Удалите faster-whisper (если установлен)

```powershell
pip uninstall faster-whisper -y
```

### Шаг 3: Используйте стандартный Whisper с оптимизированными параметрами

**В интерфейсе:**
- Модель: **medium** (качество)
- Включите **"Разделение по ролям"**
- Оптимизируйте параметры (если доступно в интерфейсе):
  - `beam_size=1` вместо 5
  - `best_of=1` вместо 5

**Ожидаемое ускорение:** 2-3x быстрее (вместо 6.6 минут → ~2-3 минуты)

---

## ✅ Решение 2: Установить hf_xet (может помочь, но рискованно)

```powershell
pip install hf-xet
```

**Проблема:** На Windows часто зависает (см. issues на GitHub).

---

## ✅ Решение 3: Подождать (рискованно)

Можно подождать 5-10 минут - возможно, модель скачается. Но если зависнет - остановите и используйте Решение 1.

---

## 🚀 Рекомендация

**Лучше использовать Решение 1:**

1. Остановите процесс (Ctrl+C)
2. Удалите faster-whisper: `pip uninstall faster-whisper -y`
3. Перезапустите сервер: `.\run_server.ps1`
4. Используйте стандартный Whisper с:
   - Модель: **medium**
   - Разделение по ролям: **включено**
   - Оптимизированные параметры (если доступно)

**Результат:**
- ✅ Работает сразу
- ✅ 2-3x быстрее с оптимизацией
- ✅ Разделение по ролям работает (простая версия)
- ✅ Нет проблем с XET

---

## 💡 Альтернатива: Использовать модель base

Если скорость все еще недостаточна:
- Используйте модель **"base"** вместо "medium"
- Качество: хорошее для большинства случаев
- Скорость: еще быстрее (но меньше качество чем medium)

---

## ✨ Итог

XET на Windows часто вызывает проблемы. **Проще использовать стандартный Whisper с оптимизацией параметров** - это даст 2-3x ускорение без проблем с XET.

**Выполните Решение 1 - это самое надежное!**


